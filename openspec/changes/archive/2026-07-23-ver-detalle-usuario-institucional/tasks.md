## 1. Backend — service (lógica) y resources

- [x] 1.1 En `app/Services/Seguridad/GestionUsuariosService`, agregar `actividadReciente(User $usuario, int $limite = 10): array` que devuelva `['negocio' => Collection<AuditLog>, 'seguridad' => Collection<SecurityAuditLog>]`, cada una filtrada por `user_id` del usuario, `orderByDesc('id')` y `limit($limite)`. Toda la consulta vive acá; el controlador no arma queries.
- [x] 1.2 En el mismo service, agregar `permisosEfectivos(User $usuario): array` que devuelva `['acceso_total' => bool, 'permisos' => string[]]`: `acceso_total` es `true` cuando el usuario tiene el rol `superadmin` (y en ese caso `permisos` va vacío, sin enumerar `Permission::all()`); en cualquier otro caso `permisos` son los nombres de `getAllPermissions()` ordenados. Sin pasar por `PermisosCompartidosResolver` ni por su caché (decisión 3 del design).
- [x] 1.3 Crear `app/Http/Resources/Seguridad/SecurityAuditLogResource` exponiendo `id`, `event`, `description`, `ip_address`, `user_agent` y `created_at`, siguiendo la forma de `AuditLogResource` (que ya existe y se reutiliza tal cual para la actividad de negocio).

## 2. Backend — ruta y controlador

- [x] 2.1 En `routes/seguridad.php`, agregar `Route::get('{usuario}', [UserController::class, 'show'])->name('show')` dentro del grupo `usuarios.`, declarada **después** de `create` y de `store` (decisión 1 del design: si va antes, `/usuarios/create` se resuelve contra `{usuario}` y devuelve 404).
- [x] 2.2 En `app/Http/Controllers/Seguridad/UserController`, agregar `show(Request $request, User $usuario): Response` que: autorice con `Gate::authorize('view', $usuario)` (recién acá queda en uso `UserPolicy::view()`), haga `$usuario->load(['roles', 'funcionario.cfinanciero.jurisdiccion', 'funcionario.ccosto'])` (las mismas relaciones que `index`, para que `UserResource` sirva sin cambios), delegue en los dos métodos nuevos del service, y renderice `seguridad/usuarios/show` con: `usuario` (`UserResource`), `permisos_efectivos`, `actividad` (`AuditLogResource::collection(...)` y `SecurityAuditLogResource::collection(...)`) y `permissions` con las mismas claves que ya usa `index` (`can_edit_user`, `can_activate_user`, `can_deactivate_user`, `can_reset_password`). Controlador liviano: solo autorizar, cargar y componer.
- [x] 2.3 Regenerar rutas tipadas con `php artisan wayfinder:generate --with-form` para que `usuarios.show` quede disponible en `resources/js/routes/`.

## 3. Frontend — tipos, página y menú

- [x] 3.1 En `resources/js/types/seguridad.ts`, agregar los tipos de la respuesta del detalle: `PermisosEfectivos` (`{ acceso_total: boolean; permisos: string[] }`), `AuditLogRegistro` y `SecurityAuditLogRegistro` según lo que exponen los Resources. Reutilizar el tipo de usuario ya existente para la cabecera.
- [x] 3.2 Crear `resources/js/pages/seguridad/usuarios/show.tsx` con: cabecera de identidad y estado (nombre, email, RUT, cargo, unidad, badge activo/inactivo, último acceso, fecha de creación) usando `Avatar`+iniciales (`useInitials`); bloque de ámbito institucional (jurisdicción → centro financiero → centro de costo); bloque de roles y permisos efectivos que muestre el distintivo de acceso total cuando `acceso_total` sea `true` en vez de la lista, y un estado vacío cuando no hay roles. Fallback `"—"` en todo campo opcional nulo.
- [x] 3.3 En la misma página, agregar las dos secciones de actividad reciente como tablas densas (`table-fixed text-xs`, patrón de listado denso del proyecto): negocio (acción, entidad afectada `auditable_type`/`auditable_id`, fecha) y seguridad (evento, descripción, IP, fecha, con `user_agent` truncado y `title`); estado vacío por sección cuando no hay registros; sin paginación.
- [x] 3.4 En la misma página, agregar las acciones de cuenta (editar, activar/desactivar según `active`, resetear contraseña) condicionadas por `permissions`, reutilizando los endpoints existentes (`usuarios.edit`, `usuarios.activar`, `usuarios.desactivar`, `usuarios.reset-password`) con el mismo diálogo de confirmación que ya usa `user-actions-menu.tsx`.
- [x] 3.5 En `resources/js/components/seguridad/user-actions-menu.tsx`, eliminar el arreglo `diferidas` y su bloque de renderizado con tooltip "Disponible próximamente", y reemplazarlo por un `DropdownMenuItem` activo "Ver detalle" condicionado a `permissions.can_view_user` que navegue a `usuarios.show(usuario.id)`. Verificar que no queden imports de `Tooltip*` sin uso en el archivo.

## 4. Tests y validaciones

- [x] 4.1 Crear `tests/Feature/Seguridad/DetalleUsuarioTest.php` cubriendo: usuario con permiso `usuarios.ver` recibe la página con identidad, ámbito institucional, roles y permisos efectivos; usuario sin el permiso es bloqueado y queda el registro en `security_audit_logs`; usuario sin `Funcionario` responde 200 con los campos institucionales en `null`; superadmin devuelve `permisos_efectivos.acceso_total = true` con `permisos` vacío.
- [x] 4.2 En el mismo archivo, cubrir la actividad: un usuario con registros propios y ajenos en ambas tablas de auditoría devuelve **solo** los propios, y un usuario sin actividad devuelve ambas colecciones vacías. Afirmar también que la petición no crea filas nuevas en `audit_logs` ni en `security_audit_logs` (comparar `count()` antes y después) para fijar el carácter de solo lectura.
- [x] 4.3 Agregar el test de regresión de orden de rutas que exige el design (riesgo 4): pedir `usuarios.create` con permiso `usuarios.crear` y esperar 200, de modo que si alguien mueve `usuarios.show` por encima de `create` en un refactor futuro, CI lo detecte.
- [x] 4.4 Correr `php artisan test --compact tests/Feature/Seguridad/` y dejarlo verde. Si aparecen fallas por helpers Pest definidos en otros archivos, correr la suite completa (`php artisan test`) para confirmar que son el gotcha conocido y no una regresión.
- [x] 4.5 Correr `vendor/bin/pint --dirty --format agent`, `composer types:check` (PHPStan), `npm run types:check` y `npm run lint:check` — todo verde antes de dar por cerrada la implementación.
