## 1. Base de datos

- [x] 1.1 Migración `create_contratos_table`: `codigo` (unique), `modalidad_compra`, `id_proceso_mp` (nullable), `tipo_contrato`, `referencia` (text), `fecha_inicio_vigencia`, `fecha_fin_vigencia`, `proveedor_id` (FK), `materia` (nullable), `submateria` (nullable), `tiene_convenio_precio` (boolean, default false), `tiene_calendario_pago` (boolean, default false), `periodicidad_pago` (nullable), `monto_total` (decimal, nullable), `proceso_adquisicion_id` (FK nullable, nullOnDelete), `licitacion_mercado_publico_id` (FK nullable, nullOnDelete), timestamps.
- [x] 1.1b Migración incremental `add_id_institucional_to_contratos_table`: agrega `id_institucional` (`unsignedBigInteger`, `unique` → indexado, obligatorio) — el identificador que la institución usa para todo efecto sobre el contrato (ver design.md decisión 6), distinto del `id` interno y del `id_proceso_mp` de Mercado Público. Validado en `StoreContratoRequest`/`UpdateContratoRequest`, expuesto en `ContratoResource`, incluido en la búsqueda del listado (`ContratoController::index`, con `CAST(... AS TEXT)` portable entre Postgres y SQLite).
- [x] 1.2 Migración `create_contrato_items_convenio_precio_table`: `contrato_id` (FK cascade), `descripcion` (text), `unidad_medida`, `precio_unitario` (decimal), `moneda`, `vigente_desde` (date nullable), `vigente_hasta` (date nullable), timestamps.
- [x] 1.3 Migración `create_contrato_cuotas_table`: `contrato_id` (FK cascade), `numero_cuota` (integer), `fecha_vencimiento` (date), `monto` (decimal), `moneda`, `estado` (`pendiente`/`pagada`, default `pendiente`), `caso_pago_proveedor_id` (FK nullable, nullOnDelete), timestamps; índice compuesto `contrato_id`+`numero_cuota` unique.
- [x] 1.4 Migración `add_contrato_id_to_ordenes_compra_mercado_publico_table`: agrega `contrato_id` (FK nullable, nullOnDelete) a `ordenes_compra_mercado_publico`.
- [x] 1.5 Seed de `DefinicionWorkflow` código `contratos` (estados `borrador`, `pendiente`, `aprobado`, `rechazado`; transiciones `pendiente`, `aprobar`, `rechazar`), siguiendo `database/seeders/WorkflowAdquisicionesSeeder.php` como plantilla de estructura.
- [x] 1.6 Agregar permisos `contratos.crear`, `contratos.editar`, `contratos.aprobar`, `contratos.rechazar`, `contratos.ver`, `contratos.vincular_pago` (convención `modulo_accion.verbo`) al seeder de permisos de Adquisiciones vigente, vía `givePermissionTo` (aditivo, idempotente); confirmar contra `database/seeders/FuncionariosCapjSeeder.php` qué roles/cargos los reciben. (`WorkflowContratosSeeder`: `admin` recibe todos, `administrativo_adquisiciones` recibe ver/crear/editar — mapeo cargo→rol específico de contratos queda pendiente de confirmación real con el usuario, no había un cargo evidente en `FuncionariosCapjSeeder`.)

## 2. Modelos

- [x] 2.1 Crear `app/Models/Contrato.php`: fillable según migración, relaciones `proceso()` (MorphOne sobre `Proceso::sujeto`), `proveedor()`, `procesoAdquisicion()`, `licitacionMercadoPublico()`, `itemsConvenioPrecio()` (HasMany `ContratoItemConvenioPrecio`), `cuotas()` (HasMany `ContratoCuota`, orderBy `numero_cuota`), `ordenesCompraMercadoPublico()` (HasMany, inverso de `contrato_id`).
- [x] 2.2 Crear `app/Models/ContratoItemConvenioPrecio.php`: fillable según migración, relación `contrato()` (BelongsTo).
- [x] 2.3 Crear `app/Models/ContratoCuota.php`: fillable según migración, relaciones `contrato()` (BelongsTo), `casoPagoProveedor()` (BelongsTo); accessor/atributo calculado `estaVencida` (deriva `estado === pendiente && fecha_vencimiento < hoy`, sin persistir).
- [x] 2.4 Modificar `app/Models/OrdenCompraMercadoPublico.php`: agregar `contrato_id` a `$fillable` y relación `contrato()` (BelongsTo).
- [x] 2.5 Revisar/extender `app/Models/Proceso.php::descriptorNotificacion()` (líneas ~124-155) para agregar el caso `instanceof Contrato`, siguiendo el mismo patrón que los demás sujetos.

## 3. Autorización

- [x] 3.1 Crear `app/Policies/ContratoPolicy.php` siguiendo `app/Policies/ProcesoAdquisicionPolicy.php` como referencia (métodos `viewAny`/`view`/`create`/`update` mapeados a `contratos.ver`/`contratos.crear`/`contratos.editar`). (Se agregaron además `vincular` y `vincularPago` para los endpoints de vínculo, siguiendo el mismo criterio.)
- [x] 3.2 Registrar `Gate::policy(Contrato::class, ContratoPolicy::class)` en `AppServiceProvider::configureAuthorization()`.

## 4. Backend — servicio y controlador

- [x] 4.1 Crear `app/Services/Contratos/ContratoService.php`: método `crear()` (resuelve/crea proveedor por RUT normalizado reutilizando el patrón de `OrdenCompraMercadoPublicoService::resolverProveedor()`/`CAMPOS_COMPLETABLES_PROVEEDOR`, crea `Contrato` + su `Proceso` en `borrador`, todo en `DB::transaction`), método `actualizar()` (solo si `Proceso` en `borrador`), método `agregarItemConvenioPrecio()` (valida `tiene_convenio_precio = true` y estado `borrador`).
- [x] 4.2 Crear `app/Services/Contratos/ContratoCalendarioPagoService.php`: método `generarCalendario()` (calcula fechas de vencimiento según `periodicidad_pago` entre `fecha_inicio_vigencia`/`fecha_fin_vigencia`, distribuye `monto_total` entre las cuotas ajustando la última por el resto, crea las `ContratoCuota` en `DB::transaction`; rechaza si falta `monto_total` o el contrato no está en `borrador`), método `actualizarCuota()` (edita fecha/monto de una cuota individual, solo si `Contrato` en `borrador`).
- [x] 4.3 Extender `ContratoCalendarioPagoService` con `vincularPago()`/`desvincularPago()`: asigna/quita `caso_pago_proveedor_id` en la `ContratoCuota` y actualiza su `estado` (`pendiente`↔`pagada`), registrando `AuditLogger::log()`. (Se colocó en `ContratoCalendarioPagoService` en vez de `ContratoService` por cohesión — todo lo relativo a cuotas vive junto.)
- [x] 4.4 Crear `app/Http/Controllers/Contratos/ContratoController.php` (index/store/show/update) — controlador liviano, delega toda la lógica no trivial a `ContratoService`.
- [x] 4.5 Crear `app/Http/Controllers/Contratos/ContratoItemConvenioPrecioController.php` (store/destroy de ítems) delegando a `ContratoService`.
- [x] 4.6 Crear `app/Http/Controllers/Contratos/ContratoCuotaController.php` (generar calendario, update de cuota individual, vincular/desvincular pago) delegando a `ContratoCalendarioPagoService`.
- [x] 4.7 Crear Form Requests: `StoreContratoRequest`, `UpdateContratoRequest`, `StoreContratoItemConvenioPrecioRequest`, `GenerarCalendarioPagoRequest`, `UpdateContratoCuotaRequest`, `VincularPagoContratoCuotaRequest` con las validaciones de campos obligatorios. (Se agregaron además `VincularProcesoAdquisicionContratoRequest`, `VincularLicitacionMercadoPublicoContratoRequest` y `VincularContratoOrdenCompraMercadoPublicoRequest` para los endpoints de vínculo de la sección 4.9, no listados explícitamente aquí pero necesarios para completarla.)
- [x] 4.8 Endpoint de transición de estado del `Contrato`: reutilizar el controlador/endpoint genérico de transiciones de workflow existente (el mismo que usa Adquisiciones/CDP) apuntando a `TransicionWorkflowService::execute()` — no crear lógica de transición nueva. (Se creó `TransicionContratoController` reutilizando `EjecutarTransicionRequest` de Adquisiciones, que es genérico — no había un controlador único compartido entre dominios.)
- [x] 4.9 Endpoints de vínculo/desvínculo `Contrato` ↔ `ProcesoAdquisicion`/`LicitacionMercadoPublico`, `OrdenCompraMercadoPublico` ↔ `Contrato`, y `ContratoCuota` ↔ `CasoPagoProveedor`, cada uno registrando la acción vía `AuditLogger::log()`.
- [x] 4.10 Crear `app/Http/Resources/Contratos/ContratoResource.php`, `ContratoItemConvenioPrecioResource.php` y `ContratoCuotaResource.php` (incluye el atributo derivado `esta_vencida`) para el payload hacia React.
- [x] 4.11 Rutas nuevas en `routes/contratos.php`, requerido desde `routes/web.php` (patrón de los demás dominios). (El vínculo OC↔Contrato vive en `routes/adquisiciones.php`, junto al resto de rutas de OC.)

## 5. Checklist documental

- [x] 5.1 Confirmar/crear el `conjunto_requisitos_documentales` para el proceso `contratos` (reutilizando el `tipo_documento` código `CONTRATO` ya existente), resuelto por `ResolutorChecklistDocumentalProceso`. (`RequisitosDocumentalesContratosSeeder`, sin distinción por `modalidad_id` ya que el requisito aplica parejo a todas las modalidades de contrato.)

## 6. Frontend

- [x] 6.1 Crear `resources/js/pages/contratos/index.tsx` siguiendo el patrón de "listado denso" de `resources/js/pages/maestros/cfinancieros/index.tsx` (columnas fijas, badge de estado, búsqueda debounce 300ms, paginación simple).
- [x] 6.2 Crear `resources/js/pages/contratos/create.tsx` / `edit.tsx` (formulario). Los ítems de convenio de precio y la generación del calendario de cuotas quedaron en `show.tsx` en vez de en create/edit — el backend (`ContratoService::agregarItemConvenioPrecio`, `ContratoCalendarioPagoService::generarCalendario`) solo opera sobre un `Contrato` ya existente, igual patrón que CDP (líneas presupuestarias se administran después de crear).
- [x] 6.3 Crear `resources/js/pages/contratos/show.tsx` (detalle, checklist documental recibido del backend, acciones de transición de workflow, vínculos con proceso de adquisición/licitación, y tabla del calendario de cuotas con estado pendiente/pagada/vencida y acción de vincular/desvincular caso de pago).
- [x] 6.4 Agregar ítem de sidebar "Contratos" condicionado a `auth.permissions` (`contratos.ver`), dentro del grupo "Adquisiciones".
- [x] 6.5 En el flujo de OC de Mercado Público (página de detalle de OC), agregar selector opcional para vincular/desvincular un `Contrato`. Se agregó también `contrato_id`/`contrato` a `OrdenCompraMercadoPublicoResource` y al `show()` del controlador (no estaban expuestos).
- [x] 6.6 Regenerar Wayfinder (`php artisan wayfinder:generate --with-form`) tras agregar las rutas/controladores nuevos.

## 7. Tests

- [x] 7.1 `tests/Feature/Contratos/CrearContratoTest.php`: creación en borrador, validación de campos obligatorios, resolución/dedup de proveedor por RUT (existente completo, existente con campos vacíos, inexistente).
- [x] 7.2 `tests/Feature/Contratos/TransicionContratoTest.php`: borrador→pendiente→aprobado/rechazado vía `TransicionWorkflowService`, bloqueo de edición tras `aprobado`, bloqueo de transición a `aprobado` sin checklist completo.
- [x] 7.3 `tests/Feature/Contratos/ContratoItemConvenioPrecioTest.php`: crear ítems solo si `tiene_convenio_precio = true` y estado `borrador`.
- [x] 7.4 `tests/Feature/Contratos/VinculoContratoTest.php`: vincular/desvincular `Contrato` con `ProcesoAdquisicion`/`LicitacionMercadoPublico`, y `OrdenCompraMercadoPublico` con `Contrato`, verificando que no se disparan transiciones de workflow y que se audita la acción.
- [x] 7.5 `tests/Feature/Contratos/CalendarioPagoContratoTest.php`: generación automática de cuotas por periodicidad (mensual, semestral, única) sobre un rango de vigencia, distribución/ajuste de `monto_total`, rechazo sin `monto_total`, edición de cuota individual solo en `borrador`, bloqueo de generación/edición tras `aprobado`.
- [x] 7.6 `tests/Feature/Contratos/VincularPagoCuotaTest.php`: vincular/desvincular una `ContratoCuota` a un `caso_pago_proveedor`, verificando el cambio de `estado` (`pendiente`↔`pagada`), que no se dispara ninguna transición de workflow en ninguna de las dos entidades, y que se audita la acción; caso de cuota vencida (`estado = pendiente` con `fecha_vencimiento` pasada) presentada como "vencida" sin persistir ese valor.
- [x] 7.7 Actualizar `tests/Feature/Adquisiciones/*` (u otro archivo existente que cubra `OrdenCompraMercadoPublico`) para cubrir el nuevo campo `contrato_id` sin romper los tests actuales. (Cobertura del vínculo OC↔Contrato quedó en `tests/Feature/Contratos/VinculoContratoTest.php` en vez de en `tests/Feature/Adquisiciones/`, agrupada con el resto de vínculos de Contrato; la suite completa de Adquisiciones (126 tests) sigue en verde.)
- [x] 7.8 Actualizar el test que afirma la lista exacta de permisos core/Adquisiciones (si aplica, según convención documentada en CLAUDE.md) con los nuevos permisos `contratos.*`. (No aplica: `RolesAndPermissionsSeederTest` solo afirma permisos core, no incluye adquisiciones/pago_proveedores/presupuesto — confirmado por grep, ningún permiso de dominio funcional está en esa lista.)

## 8. Validación final

- [x] 8.1 `vendor/bin/pint --dirty --format agent` sobre los archivos PHP tocados.
- [x] 8.2 `composer test` (lint:check + types:check + suite Pest completa). Pint y la suite Pest (906 tests, 4 skipped preexistentes) pasan en verde. `composer types:check` (PHPStan) sigue fallando con 19 errores, pero **todos preexistentes en `master` antes de este change** (verificado con `git stash` + `phpstan analyse`, mismos 19 errores en los mismos archivos no tocados por Contratos: `RoleController`, `GestionRolesService`, `UserResource`, `ProcesoAdquisicionResource`, comandos de importación 2182). Todos los archivos nuevos/modificados de este change pasan PHPStan sin errores (`vendor/bin/phpstan analyse` acotado a `app/Models/ContratoCuota.php app/Services/Contratos app/Http/Requests/Contratos`: 0 errores).
- [x] 8.3 `npm run lint:check` y `npm run types:check` sobre el frontend tocado. Ambos en verde.
- [x] 8.4 Verificación manual en navegador: crear un contrato con convenio de precios, transicionarlo por el workflow, vincularlo a una OC existente, y confirmar que el vínculo aparece en ambos lados sin alterar sus estados de workflow. Confirmado por el usuario contra su Laragon local.
- [x] 8.5 Verificación manual en navegador: crear un contrato con calendario de pago mensual, confirmar que las cuotas se generan correctamente, editar una cuota individual en borrador, vincular una cuota a un caso de pago existente y confirmar que su estado cambia a "pagada" sin alterar el workflow del caso de pago. Confirmado por el usuario contra su Laragon local.
