## 1. Backend — controlador y Resource

- [x] 1.1 En `app/Http/Controllers/PagoProveedores/EgresoCguController::show`, cambiar `->load('items.caso')` por `->load(['items.caso.proveedor', 'items.caso.proceso.estadoActual', 'registradoPor', 'cfinanciero'])`. Solo eager-load, sin lógica adicional en el controlador.
- [x] 1.2 En `app/Http/Resources/PagoProveedores/EgresoCguResource::toArray`, ampliar la cabecera con `periodo`, `cfinanciero` (`['nombre' => $this->cfinanciero?->nombre]` o `null`), `registrado_por` (`$this->registradoPor?->name`), `generado_automaticamente` y `cantidad_casos` (`$this->items->count()`). Las relaciones de cabecera (`cfinanciero`, `registradoPor`) se guardan con `whenLoaded` para no alterar el listado (`index`).
- [x] 1.3 En el mismo Resource, ampliar el `map()` de `items` para exponer por caso: `caso.id`, `caso.sgf_id`, `numero`, `proveedor` (`['nombre' => ..., 'rutproveedor' => ...]`, guardado con `relationLoaded('proveedor')`), `periodo`, `fecha_sii`, `folio_egreso`, `observacion`, `estado_actual` (`new EstadoWorkflowResource(...)` sólo si `relationLoaded('proceso')` y hay `estadoActual`, o `null`) y `monto` (el de la línea). Guardas para no meter N+1 en el listado; `->values()` conservado.

## 2. Frontend — tipos y vista

- [x] 2.1 En `resources/js/types/pago-proveedores.ts`, extender `EgresoCguItem` (agregar `caso: { id, sgf_id }`, `numero`, `proveedor: { nombre, rutproveedor }`, `periodo`, `fecha_sii`, `folio_egreso`, `observacion`, `estado_actual: EstadoWorkflow | null`) y `EgresoCgu` (agregar `periodo`, `cfinanciero: { nombre } | null`, `registrado_por: string | null`, `generado_automaticamente: boolean`, `cantidad_casos: number`).
- [x] 2.2 En `resources/js/pages/pago-proveedores/egresos-cgu/show.tsx`, reconstruir la cabecera con una grilla compacta de metadatos (periodo, centro financiero, casos cubiertos, registrado por) y un badge "Generado automáticamente" cuando aplique, además del título/fecha/monto/glosa actuales.
- [x] 2.3 En la misma vista, reemplazar el `<ul>` de "Casos cubiertos" por una tabla densa (`table-fixed text-xs`) siguiendo el patrón de `casos/index.tsx`: celda de proveedor con `Avatar`+iniciales (`useInitials`) + nombre truncado con `title` + RUT (`font-mono text-[10px]`, fallback `sgf_id`/"—"); columnas `sgf_id`, N° DTE (`numero`), estado (`<EstadoBadge estado={item.estado_actual} compact />` con guard si es `null`), fecha SII (`formatFecha`) y `Monto` a la derecha; columnas secundarias (periodo, folio, glosa) con `hidden … lg:table-cell`; cada fila enlazada a `casos.show(item.caso.id)`; estado vacío cuando no hay ítems.

## 3. Tests y validaciones

- [x] 3.1 Ampliar `tests/Feature/PagoProveedores/DetalleEgresoCguTest.php` (o el helper `crearEgresoCguDePrueba`) para que el caso de prueba tenga proveedor, `proceso.estadoActual`, `numero` y `fecha_sii`; agregar aserciones Inertia sobre los nuevos campos por item (`egreso.items.0.proveedor.nombre`, `.numero`, `.estado_actual.codigo`) y de cabecera (`egreso.periodo`, `egreso.cfinanciero`, `egreso.registrado_por`, `egreso.cantidad_casos`), incluyendo un caso con proceso nulo → `estado_actual` null.
- [x] 3.2 Correr `php artisan test --compact tests/Feature/PagoProveedores/` (directorio completo por el gotcha de helpers Pest) y dejarlo verde. → suite completa 669 passed / 4 skipped / 0 fallas (las 2 fallas al correr solo el dir eran el gotcha de `crearTipoDocumentoDePrueba`, ajenas al cambio).
- [x] 3.3 Correr `vendor/bin/pint --dirty --format agent`, `npm run types:check`, `npm run lint:check` y `composer types:check` (PHPStan) — todo verde.
