## 1. Backend — service de descarga

- [x] 1.1 Crear `app/Services/Adquisiciones/DescargaPdfLicitacionMercadoPublicoService` (clase propia, no un método de `LicitacionMercadoPublicoService` — decisión 2 del design), inyectando `IntegracionExternaService`. Definir la constante con la URL base de la ficha pública (`https://www.mercadopublico.cl/Procurement/Modules/RFB/DetailsAcquisition.aspx`), en el mismo estilo que `URL_BASE_DETALLE_OC_MERCADO_PUBLICO` de `OrdenCompraMercadoPublicoService`.
- [x] 1.2 Implementar el paso 1 del postback: `GET` a `DetailsAcquisition.aspx?idlicitacion=<codigo>` con `Http::withOptions(['cookies' => ...])` para conservar la sesión, siguiendo el redirect 302 a `?qs=<token>`. Extraer del HTML `__VIEWSTATE` y `__VIEWSTATEGENERATOR` y verificar la presencia del control `descargar_pdf`. Si el control no está, retornar "no encontrado" sin intentar el `POST`.
- [x] 1.3 Implementar el paso 2: `POST` a la URL final (la del redirect, no la original) con el mismo cookie jar y los campos `__EVENTTARGET=descargar_pdf`, `__EVENTARGUMENT=''`, `__VIEWSTATE` y `__VIEWSTATEGENERATOR`. Nota: esta página **no** trae `__EVENTVALIDATION`; incluirlo solo si aparece. Validar que la respuesta sea `application/pdf` **y** que el cuerpo empiece con `%PDF-`; si no, descartar (escenario "La respuesta no es un PDF").
- [x] 1.4 Registrar la evidencia con `IntegracionExternaService`: `iniciarTrabajo($sistema, 'descargar_pdf_licitacion', 'scraping')` al comenzar, una `registrarSolicitud()` por cada una de las dos peticiones (con método, endpoint, estado, código HTTP, duración y error cuando corresponda) y `finalizarTrabajo()` con `completado`/`error` en todas las salidas, incluidas las de falla. Envolver ambas peticiones en `try/catch` de `Throwable` para el escenario de error de red.
- [x] 1.5 Persistir el PDF con `Storage::disk('local')` bajo un directorio propio (p. ej. `mercado-publico-pdf/`), con nombre derivado del código, y registrar el snapshot con `registrarSnapshot($sistema, 'scraping_pdf', $payloadCrudo, referenciaExterna: $codigo, trabajo: ..., solicitud: ..., vinculable: $licitacionLocal)`. El `$payloadCrudo` lleva código, URL de la ficha, nombre de archivo, content-type, tamaño en bytes, ruta relativa y **`hash_pdf`** (SHA-256 del archivo) — recordar que la columna `hash` del snapshot la calcula el service sobre el JSON del payload, no sobre el archivo (decisión 3 del design).
- [x] 1.6 Implementar la reutilización: antes de salir a la red, buscar el snapshot más reciente con `sistema_externo_id` de `MERCADO_PUBLICO` + `referencia_externa = $codigo` + `metodo_captura = 'scraping_pdf'`. Si existe **y** su archivo sigue en disco, devolver ese archivo sin ninguna petición externa. Si el registro existe pero el archivo no, seguir de largo y capturar de nuevo. Sin migración: el índice `(sistema_externo_id, referencia_externa)` ya existe.

## 2. Backend — ruta y controlador

- [x] 2.1 En `routes/adquisiciones.php`, agregar `Route::get('pdf', [LicitacionMercadoPublicoController::class, 'pdf'])->name('pdf')` dentro del grupo `licitaciones_mp.`, declarada **antes** de `Route::get('{licitacion}', ...)` para que el parámetro no capture `/pdf` — mismo orden que ya tiene el grupo `ordenes_compra_mp.`.
- [x] 2.2 En `app/Http/Controllers/Adquisiciones/LicitacionMercadoPublicoController`, agregar `pdf(...)` que autorice con `Gate::authorize('viewAny', LicitacionMercadoPublico::class)` (misma habilidad que usa el `pdf` de la OC, sin permiso nuevo), valide el `codigo` con el Form Request ya existente para búsquedas de licitación (o uno análogo si el actual no sirve para query string), delegue **todo** en el service y responda el archivo con `Content-Type: application/pdf` y `Content-Disposition` con el nombre por código. Ante falla, `back()->withErrors([...])` con un mensaje legible, igual que el `pdf` de la OC. Controlador liviano: autorizar, delegar, responder.
- [x] 2.3 Regenerar rutas tipadas con `php artisan wayfinder:generate --with-form`.

## 3. Frontend — habilitar la acción

- [x] 3.1 En `resources/js/pages/adquisiciones/licitaciones-mercado-publico/show.tsx` (línea ~248) y en `buscar.tsx` (líneas ~375 y ~488), reemplazar los tres `urlPdf={null}` por la URL del endpoint nuevo con el código como query, importando la ruta tipada igual que hacen las páginas de OC con `pdfOrdenCompraMp`. Sin cambios en el contrato del componente.
- [x] 3.2 En `resources/js/components/mercado-publico/ficha-consulta.tsx`, eliminar la rama `else` del botón "Ver PDF" (la deshabilitada con `title="Disponible próximamente"`) ahora que ningún llamador pasa `urlPdf` en `null`, cambiar el tipo de la prop a `string` y actualizar el bloque de documentación del componente, que hoy describe explícitamente ese caso. Verificar que no queden imports sin uso.

## 4. Tests y validaciones

- [x] 4.1 Crear `tests/Feature/Adquisiciones/DescargaPdfLicitacionMercadoPublicoTest.php` con `Http::fake` (patrón de `OrdenCompraMercadoPublicoServiceTest`) y `Storage::fake('local')`, cubriendo el camino feliz: la ficha falsa trae `__VIEWSTATE`, `__VIEWSTATEGENERATOR` y el control `descargar_pdf`; el `POST` responde `application/pdf` con un cuerpo `%PDF-…`; el sistema entrega el PDF, deja el archivo en disco y crea el snapshot con referencia externa, método de captura, usuario y `hash_pdf`.
- [x] 4.2 Cubrir la vinculación del snapshot: con una `LicitacionMercadoPublico` local guardada, el snapshot queda vinculado a ella (`vinculable_type`/`vinculable_id`); sin ella, el PDF se entrega igual y el snapshot queda con el código como referencia externa y sin vinculable, **sin** crear la Licitación.
- [x] 4.3 Cubrir la reutilización: una segunda solicitud del mismo código entrega el archivo sin ninguna petición HTTP nueva (`Http::assertSentCount`), y no crea un snapshot adicional. Y el caso degradado: si el registro existe pero el archivo fue borrado del disco, el sistema vuelve a capturar en vez de fallar.
- [x] 4.4 Cubrir los tres modos de falla: ficha sin el control `descargar_pdf`, respuesta del `POST` que no es `application/pdf`, y excepción de red. En los tres, verificar que el usuario recibe el error, que la `solicitud_api_externa` queda con el estado correcto, que el `trabajo_integracion` queda cerrado como fallido (nunca en `en_progreso`) y que **no** se persiste ningún archivo ni snapshot.
- [x] 4.5 Cubrir la ruta y la autorización: un usuario con permiso de consulta de licitaciones obtiene el PDF; uno sin permiso es bloqueado. Agregar además el test de regresión de orden de rutas: pedir `adquisiciones.licitaciones_mp.pdf` y verificar que resuelve al método `pdf` y no al de `show` con `{licitacion}` = `"pdf"`.
- [x] 4.6 Correr `php artisan test --compact tests/Feature/Adquisiciones/` y dejarlo verde. Si aparecen fallas por helpers Pest definidos en otros archivos, correr la suite completa para confirmar que son el gotcha conocido y no una regresión.
- [x] 4.7 Correr `vendor/bin/pint --dirty --format agent`, `composer types:check` (PHPStan), `npm run types:check` y `npm run lint:check` — todo verde antes de dar por cerrada la implementación.
